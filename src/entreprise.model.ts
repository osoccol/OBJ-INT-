// MODEL
export interface Entreprise {
  nom: any; // Donnez le type adapté
  prestas: any; // Donnez le type adapté
  clients: any; // Donnez le type adapté
  capital: any; // Donnez le type adapté
}

// export PME implements Entreprise

// export GrandeEntreprise implements Entreprise
